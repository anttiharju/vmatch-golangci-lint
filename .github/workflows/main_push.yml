name: Main Push
on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  vmatch:
    uses: ./.github/workflows/vmatch.yml

  release:
    name: Release
    needs: vmatch
    runs-on: ubuntu-24.04
    steps:
      - name: Generate docs token
        uses: actions/create-github-app-token@v1
        id: generate-token
        with:
          app-id: ${{ secrets.AUTOMATION_APP_ID }}
          private-key: ${{ secrets.AUTOMATION_APP_PRIVATE_KEY }}
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ steps.generate-token.outputs.token }}
      - name: Release
        uses: anttiharju/actions/release@v0
      - name: Output Go details
        id: output-go-details
        uses: ./.github/actions/output-go-details
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          cache: false # Suppress warning about a 'missing' go.sum
      - name: Publish docs
        uses: ./.github/actions/publish-docs
    outputs:
      homepage: ${{ steps.output-go-details.outputs.homepage }}
      url: ${{ steps.output-go-details.outputs.url }}
      go_version: ${{ steps.output-go-details.outputs.go_version }}
      version: ${{ steps.output-go-details.outputs.version }}

  distribute:
    name: Distribute
    needs: release
    uses: ./.github/workflows/distribute.yml
    with:
      homepage: ${{ needs.release.outputs.homepage }}
      url: ${{ needs.release.outputs.url }}
      go-version: ${{ needs.release.outputs.go_version }}
      version: ${{ needs.release.outputs.version }}
    secrets: inherit
