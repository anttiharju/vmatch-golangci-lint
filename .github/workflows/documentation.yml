name: Documentation
on:
  push:
    branches:
      - main
    paths:
      - 'docs/**/*'
  pull_request:
    paths:
      - 'docs/**/*'

jobs:
  maintain:
    runs-on: ubuntu-24.04
    steps:
      - name: Validate docs
        uses: ./.github/actions/validate-docs