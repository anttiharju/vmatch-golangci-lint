pre-push:
  jobs:
    - name: build
      run: go build
    - name: lint
      glob: "*.go"
      run: golangci-lint run

pre-commit:
  jobs:
    - name: lint
      glob: "*.go"
      run: golangci-lint run --fast --fix {staged_files}
      stage_fixed: true
