pre-push:
  jobs:
    - name: build
      run: go build

pre-commit:
  jobs:
    - name: golangci-lint
      glob: "*.go"
      run: golangci-lint run --fast --fix {staged_files}
      stage_fixed: true
